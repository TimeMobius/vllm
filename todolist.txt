# RWKV7 vLLM-SP 性能优化追踪

> **最后核对：2026-08-21（Asia/Shanghai，x=32 exact direct-cache full fusion 为稳定路径）**
> 2026-08-21：保留 strict direct-cache `sa + update + output` single-state-read fusion：服务精度为零差异，C=128 aggregate TPS `1365.38 → 1541.71`（+12.91%）。
> 同日完成 x=16 CTA 精度/性能调优：算子与服务均零差异，但同会话 C=128 仅 +0.48%、C=1 仅 +0.06%，不保留；稳定服务继续使用 x=32 full-fusion 路径。
> 同日保留 BF16 model-dtype shift cache：recurrent state 仍为 FP32；8×16 service trace 字节级零差，C=1 `27.394 → 27.603 TPS`（+0.76%）、C=128 `1542.50 → 1566.23 aggregate TPS`（+1.54%），并节省 `999,424 B/state slot`。
> 同日保留 C=1 multi-stream R/K/V projection：不改变任一 BF16 `F.linear` 数学，仅重叠三个独立 GEMV；8×16 service trace 字节级零差，短/长 C=1 分别 +1.17% / +1.20%。
> 工作目录：`/mnt/data/Codes/RWKV/vllm/vllm-rwkv7-sp-fast`
> 分支：`codex/rwkv7-sp-fast`
> 目标：在严格数值对齐和回归测试门禁下，提高 RWKV7 的 **Full Decode CUDA Graph** 和高并发 decode 吞吐。

本文件是下一轮优化的单一追踪入口。不要仅凭 microbenchmark 保留优化：每个候选都必须完成算子/模型精度、服务级 logprob、C=128 真实并发和 focused 回归测试；没有稳定收益或发生递归漂移的候选必须记录并退出默认路径。

## 1. 当前稳定状态

- [x] Full Decode CUDA Graph 已验证并运行：capture sizes 为 `1,2,4,8,16,32,64,128`。
- [x] 当前稳定服务在 `http://127.0.0.1:8030`（局域网地址：`http://192.168.10.121:8030`）。
- [x] `2026-08-21` 已验证 `/health` 返回 HTTP 200，`/v1/models` 返回 `xiaoke-5`。
- [x] dashboard 测试服务为 `http://192.168.10.121:3000`；最近一次 POST 于 **2026-08-21 14:44:19** 成功结束、保存 8 条记录，所有点成功率 100%：C=1 输出/整体 `36.4 / 35.8 t/s`（3 requests / 2,023 tokens），C=128 每请求输出/整体 `17.0 / 16.3 t/s`（128 requests / 84,660 tokens）。它验证 stable x=32 full-fusion 的外部 OpenAI 并发服务未崩溃；该 1→128 sweep 的每请求输出速度不能与本地 C=128 closed-loop aggregate TPS 直接混用。
- [x] 使用 NVIDIA GeForce RTX 4090 D（sm89）、BF16 projection、FP32 recurrent state；Python 统一使用 vllm-sp 环境。

### 稳定启动命令

```bash
cd /mnt/data/Codes/RWKV/vllm/vllm-rwkv7-sp-fast

CUDA_VISIBLE_DEVICES=0 \
VLLM_ALLOW_LONG_MAX_MODEL_LEN=1 \
VLLM_DISABLE_COMPILE_CACHE=1 \
RWKV7_USE_EXACT_RECURRENT_T1_UPDATE=1 \
RWKV7_USE_EXACT_RECURRENT_T1_OUTPUT_REDUCTION=1 \
RWKV7_USE_EXACT_RECURRENT_T1_DIRECT_CACHE=1 \
RWKV7_USE_EXACT_RECURRENT_T1_FULL_FUSION=1 \
RWKV7_USE_NATIVE_STRIDED_GATHER=1 \
RWKV7_USE_NATIVE_STRIDED_MASKED_STORE=1 \
RWKV7_USE_TRITON_MASKED_STORE=1 \
RWKV7_USE_MODEL_DTYPE_SHIFT_CACHE=1 \
RWKV7_USE_MULTISTREAM_RKV_PROJECTIONS=1 \
RWKV7_USE_MULTISTREAM_AUX_PROJECTIONS=1 \
RWKV7_USE_MULTISTREAM_BULK_DECODE=1 \
RWKV7_USE_FUSED_CAST_KK_PRE=1 \
/mnt/data/anaconda3/envs/vllm-sp/bin/vllm serve \
  /hikscale/models/XiaoKe/rwkv-0-hf \
  --served-model-name xiaoke-5 \
  --reasoning-parser rwkv \
  --enable-auto-tool-choice \
  --tool-call-parser rwkv \
  --dtype bfloat16 \
  --host 0.0.0.0 \
  --port 8030 \
  --max-model-len 1M \
  --max-num-seqs 128 \
  --gpu-memory-utilization 0.92 \
  --enable-prefix-caching \
  --mamba-cache-mode align \
  --compilation-config \
  '{"cudagraph_mode":"FULL_DECODE_ONLY","cudagraph_capture_sizes":[1,2,4,8,16,32,64,128]}'
```

检查服务：

```bash
curl -fsS http://127.0.0.1:8030/health
curl -fsS http://127.0.0.1:8030/v1/models
screen -ls
```

### 2026-08-21 C=1 balanced auxiliary projection streams（已保留）

- [x] 新增显式开关 `RWKV7_USE_MULTISTREAM_AUX_PROJECTIONS=1`：只在 TP=1、direct linear、**`forward_decode_batch` 的 batch=1** CUDA 投影路径触发（单 token prefill 也显式保持单流）。复用已有三个持久 child streams，将 mix6 后完全独立的 BF16 分支按 `R+G`、`K+W+A`、`V+V-gate`（非首层）均衡调度；随后仍在原 main stream 上执行原有 BF16→FP32、`kk_pre`、严格 FP32 recurrent/direct-cache、tail 与 `o_proj`。每一个 `F.linear`、激活、sigmoid、scale、`lerp`、dtype 和 reduction contract 均不变；batch > 1（含 C=128 Full Decode replay）保持稳定单流路径。
- [x] 严格门禁：layer 0 与 layer 1 CUDA BF16 recurrent-input bundle 反复 `torch.equal`；真实服务 8 prompts × 16 greedy、`logprobs=20` trace byte-identical；全部 Full Decode graph size `1,2,4,8,16,32,64,128` 重新 capture 成功；focused suite `93 passed, 5 skipped`。
- [x] 配对固定负载：C=1 32-token `27.9366 → 28.4571 TPS`（**+1.86%**）；512-token steady `27.9596 → 28.4891 TPS`（**+1.89%**）。C=128 实测 `1567.52 aggregate TPS`，该 gate 不触发，不能归因。保留原因是零精度漂移下可复现的 C=1 改善；仍远未达到 50+ TPS，主瓶颈依旧是大量严格 BF16 GEMV。证据：`benchmark_records/20260821_multistream_aux_projection_full_graph/`。
- [x] 外部 dashboard POST 于 `2026-08-21 16:45:52` 成功完成、保存 8 条记录：C=1/2/4/8/16/32/64/128 均 100% success；每请求输出 TPS `36.2/31.2/34.2/32.5/30.8/26.2/22.2/17.3`，整体 TPS `35.6/30.7/33.5/31.8/30.0/25.4/21.5/16.8`。这是服务 API 回归门禁；completion 数量可变，不能与 fixed-payload C=128 aggregate TPS 混用。

### 2026-08-21 deferred G-tail join（拒绝）

- [x] 试验 `RWKV7_DEFER_MULTISTREAM_G_JOIN=1`：在已保留的 C=1 balanced auxiliary streams 中，R 完成后先 record event，G LoRA 与主流的 BF16→FP32 / `kk_pre` / exact FP32 recurrent 尝试重叠，只在 attention tail 前 join。CUDA unit 的 projection bundle / `forward_decode_batch` 均 `torch.equal`，8×16 `logprobs=20` 服务 trace byte-identical，Full Decode graph `1..128` capture 成功。
- [x] 但 C=1 32-token 仅 `28.4558 → 28.4594 TPS`（+0.013%，低于 1% materiality gate、属于噪声）；已完全 restore source 与启动环境开关，不保留额外 event/state 复杂度。证据：`benchmark_records/20260821_deferred_g_join_rejected/`。

### 2026-08-21 main-stream R+G / two child streams（拒绝）

- [x] 试验 `RWKV7_USE_MAIN_R_G_PROJECTIONS=1`：把 `R+G` 留在 current stream、只用两个 child streams 执行 `K+W+A` 和 `V+V-gate`，以减少每层一组 fork/join；不改变 BF16 `F.linear`、LoRA activation、dtype 或 reduction。服务 8×16 `logprobs=20` trace byte-identical，Full Decode graph `1..128` capture 成功。
- [x] C=1 32-token 却从 `28.4558 → 28.4488 TPS`（-0.024%）。减少 event 开销抵不过 launch concurrency 变化，低于 materiality gate；已完整 restore source 与启动 flag，继续保留三个 child streams 的稳定实现。证据：`benchmark_records/20260821_main_stream_rg_rejected/`。

### 2026-08-21 `--max-num-batched-tokens 8192`（拒绝）

- [x] 为排查高并发 scheduler 是否受默认 2048 token budget 限制，测试 `--max-num-batched-tokens 8192`（128 请求 × 49 prompt tokens = 6,272 prompt tokens）。数学路径未变：8×16 `logprobs=20` service trace byte-identical，Full Decode graph `1..128` capture 成功。
- [x] 固定负载 C=1 `28.4540 → 28.4827 TPS`（+0.10%，噪声），C=128 aggregate `1568.15 → 1565.60 TPS`（-0.16%）。scheduler budget 不是当前高并发瓶颈；已移除启动 override，保持默认 2048。证据：`benchmark_records/20260821_max_num_batched_tokens_8192_rejected/`。

### 2026-08-21 `--max-num-batched-tokens 4096`（拒绝）

- [x] 在 8192 已轻微回退后补测中间 scheduler budget `4096`，确认不是极端 buffer 配置才导致回退；8×16 `logprobs=20` trace byte-identical，Full Decode graph `1..128` capture 成功。
- [x] C=1 `28.4540 → 28.4699 TPS`（+0.06%，噪声），C=128 aggregate `1568.15 → 1560.45 TPS`（-0.49%）。2048/4096/8192 三点均证实默认 2048 是本固定 128 并发 workload 的最佳 tested scheduler budget；已恢复启动命令默认值。证据：`benchmark_records/20260821_max_num_batched_tokens_4096_rejected/`。

### 2026-08-21 `mamba-cache-mode=all` memory gate（拒绝）

- [x] 为评估 upstream Mamba/Kimi 的 cache-all 生命周期是否改善高并发，使用稳定服务的 `--max-model-len 1M`、`--gpu-memory-utilization 0.92` 仅把 `--mamba-cache-mode align` 改为 `all`。模型加载后只剩 15.47 GiB cache memory，但 all-mode 要求 3,965.0 GiB 才能为 1,048,576 token 的单个请求分配 state checkpoint，engine 在 CUDA graph / HTTP benchmark 前拒绝启动，估算可支持的 max length 仅 4,080。
- [x] 这不是精度回退，而是资源/服务契约不兼容；为了让 all-mode 启动而降到 4k context 不可与当前 1M 服务比较。启动命令已恢复 `align`，不保留 all-mode。证据：`benchmark_records/20260821_mamba_cache_all_memory_rejected/`。

### 2026-08-21 C=1 multi-stream R/K/V projection（已保留）

- [x] 新增显式开关 `RWKV7_USE_MULTISTREAM_RKV_PROJECTIONS=1`：仅对 TP=1、direct linear、**decode batch=1** 的独立 R/K/V BF16 `F.linear`，在三个 lazy CUDA child streams 并行提交。main stream 先 record readiness event，三个 branch 完成后全部 wait 才进入 recurrence；F.linear 的 input / weight / bias / dtype / reduction contract 完全不变。batch > 1 回退稳定单 stream，避免把 stream synchronization 开销带到高并发 graph replay。
- [x] 严格门禁：isolated BF16 R/K/V 每个 output `torch.equal`；真实 8 prompts × 16 greedy、`logprobs=20` response trace byte-identical；Full Decode graph 1..128 全部 capture；focused RWKV suite `93 passed, 5 skipped`。
- [x] 成对固定负载：C=1 32-token `27.611 → 27.934 TPS`（+1.17%），512-token steady `27.628 → 27.960 TPS`（+1.20%）；C=128 因 feature inactive 保持基线量级（`1563.04 → 1571.71 aggregate TPS`，不归因）。该项解决的是 C=1 GEMV overlap 的一小部分，不能虚报为达到 50+ TPS；证据：`benchmark_records/20260821_multistream_rkv_projection_full_graph/`。
- [x] 外部 dashboard POST 于 `2026-08-21 16:23:03` 成功完成并保存 8 条记录：C=1/2/4/8/16/32/64/128 均 100% success；每请求输出 TPS `32.5/29.3/33.9/32.8/31.5/26.3/22.7/17.5`，整体 TPS `32.0/28.8/33.3/32.1/30.7/25.6/22.0/16.8`。这是 OpenAI API 并发稳定性门禁；生成 token 数可变，不能和 fixed-payload C=128 aggregate TPS 直接比较。

### 2026-08-21 BF16 model-dtype shift cache（已保留）

- [x] 新增显式开关 `RWKV7_USE_MODEL_DTYPE_SHIFT_CACHE=1`：attention / FFN 的 token-shift cache 从旧的 FP32 改为模型 dtype（稳定服务为 BF16）；FP32 recurrent matrix state 不变。实现同时更新了模型实例的 `get_state_dtype()` 和 engine 初始化前的 `get_mamba_state_dtype_from_config()`，避免 cache spec 与实际 block layout 不一致。
- [x] 严格门禁：isolated BF16 四步 decode 的每个返回 tensor / final cache 均 `torch.equal`；真实服务 8 prompts × 16 greedy、`logprobs=20` 的完整 trace 文件 byte-identical（text / token IDs / selected 与 Top-K logprobs 均零差）；focused RWKV suite `92 passed, 5 skipped`。
- [x] fixed payload 成对 A/B：C=1 `27.3938 → 27.6030 TPS`（+0.76%），C=128 aggregate `1542.5043 → 1566.2303 TPS`（+1.54%；final restart 为 1572.816）；每 state slot 节省 999,424 B（128 slots 为 122 MiB）。这是 Full Decode graph 的严格有效高并发优化，已启用到 8030 启动命令。C=1 仍受约 82% dense BF16 GEMV 时间限制，不能将该项误述为 50 TPS 方案。证据：`benchmark_records/20260821_model_dtype_shift_cache_full_graph/`。
- [x] 外部 dashboard POST（`2026-08-21 15:29:59`）成功完成并保存 8 条记录，C=1/2/4/8/16/32/64/128 全部 100% success；每请求输出 TPS 为 `37.8/33.0/34.3/33.7/31.5/26.8/22.7/17.8`，整体 TPS 为 `37.2/32.4/33.4/32.9/30.7/26.0/21.9/17.2`。它是 OpenAI API 并发稳定性门禁；因生成 token 数变化，禁止和 fixed-payload C=128 aggregate TPS 直接比较。

### 2026-08-21 paired shift-cache gather/store（已拒绝）

- [x] 尝试将 attention-shift 与 FFN-shift 的同 dtype/same-shape native gather 合并为一个 CUDA copy kernel，并将两个 masked store 合并；FP32 recurrent matrix 不参与。CUDA FP32/BF16、padded outer stride、Full Graph padding-slot unit 均通过，真实 8×16 `logprobs=20` service trace byte-identical。
- [x] **不保留**：paired fixed-payload A/B 为 C=1 `27.613 → 27.609 TPS`（-0.01%）、C=128 aggregate `1569.19 → 1571.26 TPS`（+0.13%），低于噪声/1% materiality 门限。源码和运行开关已撤出稳定路径；结论是继续压缩 shift copy launch 不是当前吞吐主杠杆。证据：`benchmark_records/20260821_shift_pair_cache_rejected/`。

### 2026-08-21 RWKV tokenizer invalid-UTF-8 panic preflight（已保留）

- [x] 真实 checkpoint vocab 的 65,534 个 base token 中有 486 个 UTF-8 byte fragment。旧 `pyrwkv_tokenizer` 对不完整 byte stream 会先 Rust panic、再 warning、最后才走原有 Python replacement-character fallback；此前 dashboard 一轮出现 778 次 warning。
- [x] 仅对含这些 fragment ID 的完整 base-token 序列做 UTF-8 preflight：已确定 invalid 时直接走**原有** Python fallback，合法 fragment 拼接仍走 fast decoder，added/special/unknown token 语义不变。
- [x] tokenizer suite `18 passed`；isolated 100 次 invalid-byte decode 的 Rust panic 行数 `101 → 0`；8×16 service text/token/logprob trace byte-identical；最新 dashboard POST（2026-08-21 15:02:23）1→128 全点 100% success 且 server log 0 panic warning。它消除 API detokenization/logging 开销，不改变 GPU GEMV 瓶颈；证据：`benchmark_records/20260821_rwkv_tokenizer_invalid_utf8_preflight/`。

### 2026-08-21 Full-fusion CTA 资源调优（已拒绝，不影响稳定服务）

- [x] 将 full-fusion recurrent CTA 从 `32 x 4` 做了**一致的** `16 x 4` 改写（kernel 内部和 launch 同时修改），使一个 CTA 覆盖一个 `[B,H,64]` 输出向量；shared memory `34,816 B → 17,408 B`（-50%），寄存器仍为 64/thread。
- [x] 严格精度仍通过：direct-cache B=1/8/128、十步 recurrence 均为 output/full-cache `torch.equal`；focused suite `82 passed, 4 skipped`；8×16 service `logprobs=20` 全 trace byte-identical。
- [x] **未保留**：同会话成对基准 C=128 `1537.02 → 1544.47 TPS`（+0.48%）、C=1 `27.37 → 27.38 TPS`（+0.06%），没有材料级服务收益。源码、二进制、8030 服务已恢复至 x=32 stable kernel。证据：`benchmark_records/20260821_exact_recurrent_t1_full_fusion_cta16_rejected/`。

## 2. 已保留优化（默认稳定路径）

### 2.1 精确 T=1 recurrent state update

- [x] 提交：`3d7adef20 RWKV7: fuse exact decode state update`。
- [x] 将 T=1 decode 的 FP32 state pointwise update 融合到 native CUDA，保留 ATen 的敏感 reduction（`sa` 和 output reduction）及其计算顺序。
- [x] 通过显式 FP32 round-to-nearest 的乘/加次序，避免 Albatross 风格 reduction tree 改写造成的递归状态漂移。
- [x] 服务精度门禁（8 prompts × 16 decode tokens、greedy、`logprobs=20`）：文本、token、selected/common Top-K logprob 均为 **0 差异**。
- [x] C=128 历史服务 A/B：`487.91 → 696.58 TPS`，**+42.77%**（同构建、同负载）。
- [x] 证据：`benchmark_records/20260821_exact_recurrent_t1_update_full_graph/`。

回退开关：`RWKV7_USE_EXACT_RECURRENT_T1_UPDATE=0`。

### 2.2 精确 T=1 recurrent output-reduction（新增）

- [x] 新增 native CUDA `rwkv7_reduce_d64_atten_exact`：精确匹配本环境 PyTorch
  `reduce_kernel<128,4>` 的 FP32 D=64 output-reduction 线程布局和 `4 -> 2 -> 1`
  shared-memory 加法顺序；同时保留 eager `aten::mul` 的 FP32 rounding contract。
- [x] 去掉每层 `final_state * r.unsqueeze(-1)` 的 `[B,H,64,64]` 临时张量及一个
  eager CUDA launch；只用于 strict CUDA/FP32/contiguous/[B,H,64,64] guard，其他
  布局自动 fallback。
- [x] isolated B=1/8/128、每个 5 seeds 均 `torch.equal`；服务精度 gate（8 prompts
  ×16 token、greedy、logprobs=20）为 text/token/Top-K logprob **0 difference**。
- [x] 成对服务 C=128 benchmark（每 mode 三轮，后两轮均值）:
  `706.63 → 793.18 TPS`，**+12.25%**；C=1 `25.94 → 26.00 TPS`（+0.23%，不作为
  单请求收益宣称）。证据：
  `benchmark_records/20260821_exact_recurrent_t1_output_reduction_full_graph/`。

默认开关：`RWKV7_USE_EXACT_RECURRENT_T1_OUTPUT_REDUCTION=1`；回退：设为 `0`。
### 2.3 精确 T=1 直接更新 persistent recurrent cache（新增，`15330b229`）

- [x] `2026-08-21` 新增 `rwkv7_recurrent_t1_exact_direct_cache`：对 Full Decode CUDA Graph 的 `mamba_cache_mode=align`，直接读取、严格更新并写回 persistent FP32 recurrent cache，消除每层 `[B,H,64,64]` recurrent state 的 gather 与 store。
- [x] 支持实际 vLLM cache 的 strided `[slots,H,64,64]` view（slot stride 可大于一个 state row）；仅接受内部 `(4096,64,1)` 连续布局。`slot_id=-1` 的 graph padding lane 可读取 slot 0 但绝不写 cache；`cache_all`、prefill、eager、CPU、非 FP32、非兼容 layout 自动 fallback。
- [x] 保持 existing exact FP32 contract：`sa` 和 output reduction 匹配 PyTorch `reduce_kernel<128,4>`，state update 使用显式 round-to-nearest multiply/add 顺序。isolated strided-cache B=1/8/128、3 seeds、valid output 与完整 cache 均 `torch.equal`；服务 8 prompts × 16 decode tokens、greedy、`logprobs=20` 为 text/token/selected/common Top-K **0 差异**。
- [x] 成对服务 benchmark（同一个 rebuilt `_C.abi3.so`、首轮 warm-up、后两轮均值）：C=128 `791.76 → 1205.23 TPS`，**+52.22%**；C=1 `25.91 → 26.21 TPS`（+1.13%）。这显著改善高并发，但 C=1 仍受 small BF16 projection 限制，尚未达到 50 TPS 目标。
- [x] focused regression（开启 direct-cache）：**78 passed, 2 skipped**。证据：`benchmark_records/20260821_exact_recurrent_t1_direct_cache_full_graph/`。

默认开关：`RWKV7_USE_EXACT_RECURRENT_T1_DIRECT_CACHE=1`；回退：设为 `0`。

### 2.4 精确 direct-cache state update + output-reduction fusion（`8343b8ae7`）

- [x] 在 `rwkv7_recurrent_t1_exact_direct_cache` 的 direct persistent-cache fast path 中，将原先独立的 exact state-update 和 exact output-reduction 合为一个 native CUDA kernel；敏感的 `sa` reduction 保持独立且不改变。
- [x] 新 kernel 刻意复刻既有 output reduction 的 `32 x 4` CTA、四路 FP32 accumulator 与 `4 -> 2 -> 1` shared-memory 加法树；state update 继续使用显式 round-to-nearest FP32 multiply/add 次序。对 padded graph lane，slot 0 仅用于被丢弃的读输出，永不写入 cache。
- [x] 最终服务精度 gate（8 prompts × 16 decode、greedy、`logprobs=20`）为 text/token/selected/common Top-K **0 差异**；direct-cache isolated unit B=1/8/128、3 seeds **3 passed**；完整 RWKV7 focused suite **78 passed, 2 skipped**。
- [x] clean-restart 成对服务 benchmark：C=128（128 closed-loop requests、每请求 32 token、四个非 warm-up 样本）`1252.19 → 1365.23 TPS`，**+9.03%**；C=1（首轮剔除）`26.08 → 26.22 TPS`（+0.54%，仅作 secondary）。它减少每层一个 graph node 和一次 state read/write synchronization，但 C=1 仍由 small BF16 projection 主导。
- [x] 证据：`benchmark_records/20260821_exact_recurrent_t1_direct_cache_update_reduce_full_graph/`。

默认开关不变：`RWKV7_USE_EXACT_RECURRENT_T1_DIRECT_CACHE=1`；同样设为 `0` 可回退整个 direct-cache fast path。

### 2.5 精确 direct-cache `sa + update + output` full fusion（`e5b1afd8c`）

- [x] 将 retained direct-cache 的 `sa` cache reduction 与 update/output-reduction 继续合为一个 `32 x 4` CTA CUDA kernel：每个 FP32 recurrent state element 仅从 global cache 读取一次、写回一次；original state 暂存 shared memory，避免 64-value register array 的 256-byte stack spill。
- [x] 保持严格算术合约：`sa` 与 output 仍使用既有的四路 partial accumulators、`4 -> 2 -> 1` shared-memory tree 和 FP32 round-to-nearest multiply/add；graph padding lane 仅从 slot 0 读取被丢弃输出、绝不写 cache。此路径默认启用；可设置 `RWKV7_USE_EXACT_RECURRENT_T1_FULL_FUSION=0` 回退到 2.4 的两 kernel direct-cache 路径。
- [x] 精度：single-step B=1/8/128、3 seeds 以及新增 10-step recurrent cache regression B=1/8/128 均 valid output 与完整 cache `torch.equal`；服务 8 prompts ×16 decode、greedy、`logprobs=20` text/token/selected/common Top-K **0 差异**；focused suite（先停服务以释放 GPU）**81 passed, 2 skipped**，包括两个 C=128 padded Full CUDA Graph integration mode。
- [x] clean-restart paired service benchmark（C=128 四个 non-warmup samples）`1365.38 → 1541.71 TPS`，**+12.91%**；C=1 post-warmup `26.22 → 26.35 TPS`（+0.47%，不将其误称为单流突破）。这是真正消除了 `sa` 的额外 full-state global read 与每层一个 graph node 的高并发收益。
- [x] 证据：`benchmark_records/20260821_exact_recurrent_t1_direct_cache_full_fusion_full_graph/`。

### 2.6 Full-graph state cache 的 native strided gather/store

- [x] 提交：`b30e12944 RWKV7: accelerate strided full graph state cache`。
- [x] 在 Full Decode graph 中，以 CUDA 行拷贝 gather 替换 RWKV7 block 的 state-cache `index_select`；native masked store 支持 graph replay 的 padded row stride。
- [x] unsupported layout、扩展缺失或非 CUDA 情况自动 fallback；copy-only 路径保持 dtype bits。
- [x] 服务精度门禁（8 prompts × 16 decode tokens）：文本、token、selected logprob 均为 **0 差异**。
- [x] C=128 / 128 closed-loop requests / 每请求 32 tokens：`700.675 → 708.385 TPS`，**+1.10%**（剔除每个 mode 的首轮 warm-up）。
- [x] 证据：`benchmark_records/20260821_native_strided_state_cache_full_graph/`。

回退开关：`RWKV7_USE_NATIVE_STRIDED_GATHER=0`、`RWKV7_USE_NATIVE_STRIDED_MASKED_STORE=0`。

### 2.7 编译/图模式的现状

- [x] `FULL_DECODE_ONLY` CUDA Graph 已通过真实 C=128 padded engine integration，测试覆盖普通 full decode graph 与 `RWKV7_COMPILE_WITH_FULL_CUDAGRAPH=1` 路径。
- [x] **C=1 full-model compile复测（2026-08-21）**：完整 `RWKV7_COMPILE_WITH_FULL_CUDAGRAPH=1` 服务精度仍为 text/token/logprob 0 差异，但 C=1 post-warmup `25.852 TPS` 对严格稳定路径 `25.961 TPS`（-0.42%），无稳定收益，继续保持 opt-in 不启用。证据：`benchmark_records/20260821_full_decode_compile_c1_rejected/`。
- [x] model `torch.compile` 是严格对齐的 opt-in；此前 C=128 服务均值约 `707.73 → 706.10 TPS`，2026-08-21 追加 `use_inductor_graph_partition=true` 复测也仅 `706.21 → 694.72 TPS`（剔除 candidate 首轮仍为 706.61），暂无稳定收益，**默认不打开**。
- [x] 证据：`benchmark_records/20260821_full_decode_cuda_graph_verified/`、`benchmark_records/20260821_full_decode_compile_final_norm/`、`benchmark_records/20260821_full_decode_compile_inductor_partition_rejected/`。

## 3. 已拒绝候选（不要无证据重复接入）

- [x] **Albatross 风格整块 recurrent kernel**（`csrc/rwkv7_alt_recurrent.cu`）：controlled decode microbenchmark 可约 +106.5%，但 4 prompts × 16 steps 出现 `8 / 64` Top-1 分歧，selected-logprob 最大误差 `3.1454`、common Top-K 最大误差 `9.2296`。根因是 FP32 reduction/accumulation 顺序变化在 61 层递归 decode 中放大；不能作为默认路径。
- [x] **将 `kk * a` inline 到 exact recurrent kernel**：算子 bit-exact，但 C=128 服务约 `705.61 → 705.65 TPS`，无稳定收益，已回退。
- [x] **attention/FFN cache pair gather**：microbenchmark 减少 launch，但 C=128 服务没有稳定优于独立 native gather，已回退。
- [x] **把 PAD remap 移入 native gather**：精度为零误差，但 `clamp + gather` 平均约 `707.23 TPS`，kernel remap 约 `706.57 TPS`，已回退。
- [x] **直接合并 r/k/v 权重为单一 GEMM**：B=32/128 BF16 输出不再 bit-exact（通常相差 1–2 BF16 units），不接入。
- [x] **默认 cublasGemmEx 直接替换 `F.linear`**：isolated `[M,4096] × [4096,4096]` BF16 probe 与 PyTorch bit-exact，但性能仅约 -0.22% 至 +0.34%，无接入价值。
- [x] **cublasLt heuristic/workspace 枚举（2026-08-21）**：在 `M=1/8/32/128, K=N=4096`、0/4/8/64/256 MiB workspace 的 isolated BF16 probe 中，能 `torch.equal` 的候选均与 PyTorch 基线持平量级；C=128 最快 `0.041146 ms` 对 PyTorch `0.041165 ms`（约 +0.05%，低于噪声门限），不接入模型。非精确算法在 M=8/32/128 可出现最高 1–2 BF16 units 的差异。
- [x] **BF16 ordered sparse CMix down projection（2026-08-21）**：按 Albatross 思路实现了 M=1 CUDA-graph-safe 的 tile count/prefix/ordered compact/FP32 accumulation 候选。isolated 94% sparsity时约 `0.160 → 0.111 ms`，服务 C=1 为 `25.74 → 27.38 TPS`（+6.40%），说明 FFN sparse down 的性能潜力真实存在；但 8 prompts × 16 `logprobs=20` 出现 `2 / 8` text/token 分歧、selected max `2.630697`、common Top-K max `11.437075`。FP32 accumulation 和 index 顺序均不能匹配 PyTorch BF16 GEMV reduction contract，候选已从模型代码移除并恢复严格稳定服务。证据：`benchmark_records/20260821_sparse_cmix_bf16_c1_rejected/`。
- [x] **recovered sparse CMix 单层精度扫描（2026-08-21）**：为继续压低而非直接放弃 sparse 误差，短暂链接了历史本地 object（仅诊断、绝不提交）并逐层 allowlist 测试 `0/1/30/60`。layer 60 的 text/token 均为 0 分歧，selected logprob max 仅 `1.22e-5`，但 common Top-K 仍有 `0.0156164`；layer 0 也有 nonzero logprob（selected `0.113672`、Top-K `0.374853`），layer 1/30 已各有 `1/8` text/token 分歧。layer-60 C=1 `26.2151 TPS` 对稳定 `26.2068 TPS` 仅 **+0.03%**，无实际价值。临时 CMake/binding/model改动与 rebuilt `.so` 均已恢复；这证明必须匹配 dense BF16 GEMV reduction contract，而不能靠挑选尾层保留候选。证据：`benchmark_records/20260821_sparse_cmix_source_recovery_layer_scan/`。
- [x] **ngram speculative decoding（2026-08-21，拒绝）**：尝试不改模型权重、由目标模型 verify 的 prompt-lookup ngram（4 speculative tokens，window 2–5），以评估是否能通过 speculative path 达到 C=1 目标。vLLM 会强制从 `FULL_DECODE_ONLY` 降级为 `PIECEWISE` CUDA Graph，并关闭 async scheduling；虽观测到 mean acceptance length `3.50`、draft acceptance `62.5%`，C=1 仍由稳定 `26.2068` 降至 `17.2220 TPS`（**-34.29%**）。8×16 greedy 文本/token 相同，但 selected logprob max `0.00190655`、common Top-K max `0.12647915`，不满足严格服务门禁。稳定服务已恢复。证据：`benchmark_records/20260821_ngram_speculative_rejected/`。
- [x] **`--async-scheduling` 配置 A/B（2026-08-21，不保留）**：这是不改模型计算且仍保留 `FULL_DECODE_ONLY` graph 的低风险 scheduler 候选；8×16 `logprobs=20` 严格 0 差异。clean-restart paired 结果为 C=1 post-warmup `25.9699 → 25.9774 TPS`（+0.03%），C=128 aggregate `1251.4257 → 1253.2730 TPS`（+0.15%），都低于噪声/1%门限；稳定启动命令不添加该开关。证据：`benchmark_records/20260821_async_scheduler_no_gain/`。


### 2.9 2026-08-21：fused BF16→FP32 recurrent preparation（已保留）

- [x] 新增 `RWKV7_USE_FUSED_CAST_KK_PRE=1`：将 R/W/K/A/V 的 BF16→FP32 materialization 与既有 exact Triton `k` adjust/`kk` normalize 合并，限 equal 64-wide layout；其余 layout 自动 fallback，算术表达式保持现有 kk-pre 合同。
- [x] isolated B=1/8/128 六个输出均 `torch.equal`，服务 8×16 `logprobs=20` byte-identical，Full Decode graph `1..128` 成功；focused regression **92 passed, 4 skipped**。C=1 `28.904 TPS`，C=128 四样本 **1604.05 aggregate TPS**，相对已保留 bulk stream `1584.997` 再增 **+1.20%**；clean-restart C=2/8/32 sweep 分别 `+1.36% / +1.23% / +0.99%`。
- [x] 稳定启动脚本已加入此开关；证据：`benchmark_records/20260821_fused_cast_kk_pre_retained/`。

### 3.0 2026-08-21：C=128 projection bulk multistream（已保留）

- [x] 新增显式 opt-in `RWKV7_USE_MULTISTREAM_BULK_DECODE=1`：将已有、严格保持每个 `F.linear`/LoRA 算术的三路 persistent child stream 投影拓扑从 M=1 扩展到 padded bulk decode；只在既有 RKV/AUX stream 两开关都开启时生效，prefill 不触发。
- [x] B=8 BF16 projection 单元测试 `torch.equal`，Full Decode graph sizes `1..128` 成功，服务 8×16 `logprobs=20` 与稳定 trace byte-identical；clean-restart paired C=128 `1569.495 → 1584.997 aggregate TPS`（**+0.99%**），C=1 `28.459 TPS` 无回退。
- [x] 稳定启动脚本已加入此开关；证据：`benchmark_records/20260821_multistream_bulk_decode_retained/`。

### 2.8 2026-08-21：LoRA chain torch.compile probe（拒绝）

- [x] isolated BF16 LoRA `4096→192→4096` + tanh 在 B=1/B=128 均与 eager `torch.equal`，但 compiled `67.80/83.30 us` 对 eager `30.37/34.38 us` 分别约 -55%/-59%；Inductor 未实现有效 GEMM fusion，禁止接入单个 LoRA compile wrapper。
- [x] 证据：`benchmark_records/20260821_lora_compile_probe_rejected/`。

### 3.1 2026-08-21：稳定 Full Decode C=1/C=128 profile 与 sparse 精度后续

- [x] **稳定 profile 已完成并已恢复服务**：仅在 dashboard `running=false` 的空闲窗口短暂启用 vLLM torch profiler，随后用 `/tmp/start_rwkv7_fullgraph_t1_direct_cache.sh` 恢复 8030；`/health` 200，8 prompts × 16 greedy / `logprobs=20` 与严格基线字节级零差，post-restore C=1 为 **28.4517 TPS**。
- [x] **高并发“慢”的量化解释**：C=128 不是单请求吞吐；profiled closed-loop 是 **1532.89 aggregate TPS**，折算每活跃请求约 **11.98 TPS**。每个 `generation_128` Full Decode graph GPU span 为 **73.90–75.22 ms**，因此所有 128 请求必须共同等待这一 token step；不是 scheduler/HTTP 单点卡住。
- [x] **最大收益瓶颈**：C=128 七次 graph replay 中 BF16 CUTLASS decoder projections 为 `339.377 ms`（约 **48.48 ms/graph**），exact FP32 direct-cache recurrent full fusion 为 `155.119 ms`（约 **22.16 ms/graph**）。C=1 graph GPU span 约 `35.27 ms/token`，projection 仍是关键路径；host `execute_context` 约 4 ms，单做 scheduler/K-step host 优化不足以到严格 C=1 50+ TPS。
- [x] **Albatross sparse CMix 精度继续尝试（未接入）**：临时 tiled active-index prototype 在 synthetic 94% zero 的 isolated down GEMV `166.82 → 118.36 us`（1.41x），但仍有 `2 / 4096` BF16 输出不同、max abs `2.44140625e-4`。它复现了已拒绝 ordered sparse CMix 的 reduction-contract 问题，未进入服务/仓库；下一次必须先做到 dense BF16 GEMV reduction-compatible，再进入单层→服务 logprob→Full Graph 测试链。
- [x] **upstream 审查**：已 fetch `upstream/main`，但当前 RWKV7 branch/state lifecycle 大幅分叉；MTP/speculative、attention/KV residency 等近期改动依赖模型特定 draft/KV 合同，不能 blind merge。保留可借鉴的固定 GPU metadata、graph padding/warmup/fallback 设计，不引入不等价模型代码。
- [x] **FP32 recurrent cache `float4` load/store follow-up（2026-08-21，拒绝）**：针对 profile 中第二大项，临时把 full-fusion 内每线程四个相邻 state scalar load/store 改为对齐 `float4`，并保留源码级 scalar `__fmul_rn` / `__fadd_rn` 与 reduction tree；但 8×16 service gate 出现 **8/8 text/token** 分歧、selected max `3.4346485`、common Top-K max `11.6421452`、120 个 selected logprob 非零差异。未跑 throughput 即回退源码/`.so`，恢复后 trace byte-identical。说明 recursive contract 对 compiler-visible vectorization 同样敏感；以后必须先通过 B=1/8/128 multi-step full-cache `torch.equal`。
- [x] 证据：`benchmark_records/20260821_full_decode_profile_and_sparse_followup/`。

## 4. 当前性能判断

### 4.1 C=1 与 C=128 是不同目标

- [x] 最新 strict stable service（2026-08-21、32 output tokens、temperature=0、`ignore_eos=true`）full-fusion C=1 post-warmup 为 `26.343 / 26.347 TPS`，均值 **26.35 TPS**；C=128 closed-loop aggregate 的 clean-restart paired mean 为 **1541.71 TPS**。它是 aggregate 口径，不等同于每请求输出速度。
- [x] 因此“50+ TPS”应明确为单请求 C=1 目标，而不是 aggregate C=128。C=128 已远超 50 aggregate TPS，但每请求平均约 5.51 TPS，属排队/批处理吞吐指标，不能与单流 latency 指标混用。
- [x] C=1 Torch profile（31 generation steps）表明三类 BF16 `internal::gemvx` 合计 **984.67 ms**，约占 trace 1.20 s CUDA time 的 **82%**，并有约 **426 个小 linear/step**；下一最大收益点是 decoder 小 projection/FFN down projection，不是 state cache。
- [x] checkpoint 实测 dense BF16 projection weights 每 token 至少要流过约 **23.71 GiB**（61 layers，attention direct projections、low-rank branches、FFN up/down；无精确零权重）。RTX 4090 D 的理论约 1008 GB/s 对应仅权重读取上界约 **39.6 TPS**，因此在本硬件、strict dense BF16 的 C=1 口径下不应承诺 50+；要跨越它必须实现 strict-compatible activation sparsity/压缩或使用更高带宽硬件。
- [x] 在同 checkpoint `/hikscale/models/XiaoKe/rwkv-0.pth` 和同 RTX 4090 D 上复现 Albatross C++：`--wkv32 --cmix-sparse off` 为 31.61 raw tok/s，默认 sparse CMix 为 41.15 raw tok/s（+30.2%）。它是 FP16 standalone graph，未经过 vLLM API/sampling 与 BF16 strict logprob 门禁；其 README 的 145+ TPS 是 RTX 5090 的 standalone 结果，不能直接与本机 vLLM C=1 比较。完整证据：`benchmark_records/20260821_single_decode_bottleneck_and_albatross/`。

- [x] 历史 trace（`/tmp/rwkv7_exact_profile_20260821_1616/`）显示：BF16 GEMM/CUTLASS 约 24%，ATen elementwise 约 20%，ATen reductions 约 13%，旧 cache gather/store 约 20%，exact recurrent update 约 9%。
- [x] cache-control 与 recurrent state update 的高价值安全优化已保留；C=128 新 trace 证明大 recurrent state 的 gather/store 仍占约 20.28%，但 copy-only `uint4` 不提升带宽，不能继续堆叠小 cache 算子；下一瓶颈优先是 **decoder projection GEMM/调度** 以及严格 reduction-compatible 的 state materialization 消除。
- [x] 高并发慢的主要风险不只是单个 kernel：C=128 graph replay 同时受 GEMM、逐层 launch/调度、memory traffic、padding/请求调度影响。任何优化必须以服务级 128 并发结果裁决。

## 5. 强制验证流程

### Focused 单元/集成回归

```bash
cd /mnt/data/Codes/RWKV/vllm/vllm-rwkv7-sp-fast
export VLLM_RWKV7_ENGINE_TEST_MODEL_PATH=/hikscale/models/XiaoKe/rwkv-0-hf
export RWKV7_USE_EXACT_RECURRENT_T1_UPDATE=1
export RWKV7_USE_EXACT_RECURRENT_T1_OUTPUT_REDUCTION=1
export RWKV7_USE_EXACT_RECURRENT_T1_DIRECT_CACHE=1
export RWKV7_USE_EXACT_RECURRENT_T1_FULL_FUSION=1
export RWKV7_USE_NATIVE_STRIDED_GATHER=1
export RWKV7_USE_NATIVE_STRIDED_MASKED_STORE=1

/mnt/data/anaconda3/envs/vllm-sp/bin/python -m pytest \
  tests/model_executor/test_rwkv7.py -v
```

当前最近完整 focused 结果（full-fusion direct-cache 启用、停服务释放 GPU）：**81 passed, 2 skipped**。其中包含 C=128 padded Full CUDA Graph engine integration；skip 为既有的外部 reference runtime 依赖。

### 服务精度对齐

```bash
/mnt/data/anaconda3/envs/vllm-sp/bin/python \
  /tmp/rwkv7_service_accuracy.py \
  http://127.0.0.1:8030 > /tmp/rwkv7_candidate_trace.json
```

与稳定基线 `/tmp/rwkv7_native_state_final_trace.json` 比较：`text`、`token_ids` 与 `logprobs.token_logprobs`。先解决误差再决定是否保留，不因小误差直接放弃；但只要多步递归出现 token 分歧，就不能默认启用。

### C=128 服务吞吐

```bash
/mnt/data/anaconda3/envs/vllm-sp/bin/python \
  tmp_rwkv7_remote_concurrency_bench.py \
  --base-url http://127.0.0.1:8030 \
  --model xiaoke-5 \
  --endpoint completions \
  --prompts 'Hello, how are you?' 'The capital of France is' \
            'Write a short poem about CUDA.' 'Explain attention briefly.' \
            '1 + 1 =' 'The quick brown fox' \
            'Python code to sort a list:' 'RWKV is' \
  --num-requests 128 --concurrency 128 --dispatch-mode closed_loop \
  --max-tokens 32 --temperature 0 --top-p 1 --seed 0 \
  --return-token-ids --extra-body-json '{"ignore_eos":true}' \
  --timeout-sec 300 --output-dir /tmp/rwkv7_candidate_eval \
  --run-name candidate_c128_1
```

验收：同一 build/启动参数下，每个 mode 至少 3 轮（第一轮 warm-up 不进入平均）；服务级增益应至少约 1% 且方向稳定。保存原始 summary、accuracy、comparison 和 README 到新的 `benchmark_records/YYYYMMDD_<candidate>/`。

### Dashboard / 外部测试服务

```bash
# 生成仓库内性能 dashboard
/mnt/data/anaconda3/envs/vllm-sp/bin/python \
  tools/rwkv7_perf_dashboard.py \
  --records-dir benchmark_records \
  --output benchmark_records/dashboard.html

# 用户提供的外部回归服务
curl -fsS http://192.168.10.121:3000/api/status
curl -X POST -fsS http://192.168.10.121:3000/api/run-test
curl -fsS http://192.168.10.121:3000/
```

## 6. 下一阶段优先级


1. [ ] **保持 dense BF16 reduction contract 的 sparse/structured CMix（最高优先级，C=1）**：已验证 ordered FP32 sparse GEMV 有 +6.40% 服务潜力但仍发生 recursive token divergence，不能重复接入。 近期单层 scan 进一步表明即使 layer 60 可保持 token，仍不能实现零 logprob 差且无端到端收益。下一次只探索能复用/匹配 CUTLASS tensor-core reduction 的结构化稀疏、或在不改变每个 dense projection reduction 的前提下融合/减少 projection launch；先做 `torch.equal` 与 8×16 service gate。
2. [ ] **小 projection 调度/融合（C=1）**：profile 显示约 426 个 small linear/step。优先探索可 CUDA-graph-capture 的 grouped/low-rank projection dispatch；不得拼接 r/k/v 改变数学矩阵形状。`cublasGemmBatchedEx`/strided batched 目前在 M=1 或 M=128 输出不 strict exact，不能直接接入。 ngram speculative 在当前 RWKV7 上会丢失 Full Decode CUDA Graph 且更慢；没有兼容的 draft/MTP checkpoint 前，不将其作为短期 C=1 路线。 已验证 `--async-scheduling` 也没有材料级收益，进一步排除 scheduler 作为主要 C=1 瓶颈。
3. [x] **cublasLt isolated algorithm profiling**：已覆盖 decoder/FFN `M=1/8/32/128`、0/4/8/32/64/256 MiB workspace；strict exact 候选在 C=128 与 PyTorch/CUTLASS 持平量级，未接入。
4. [x] **C=128 Full Decode trace（2026-08-21）**：10 个 `generation_128` graph replay 为 `173.95 ms/graph`（约 735.8 raw aggregate tok/s）；CUTLASS projection 23.94%、ATen elementwise 21.83%、ATen reductions 14.54%、大 FP32 recurrent-state gather/store 合计 20.28%、exact state update 9.84%。idle API 三轮为 `704.10 / 708.56 / 706.67 TPS`，后两轮均值 `707.61`；与外部 dashboard 的 1→128 sweep 重叠会显著污染 client TPS，不能混用。`uint4` copy-only A/B 不稳定且无收益，未接入。证据：`benchmark_records/20260821_c128_full_decode_profile/`。
5. [x] **官方 upstream 审查（2026-08-21）**：已 fetch 且核实远端 `upstream/main=a60c66e3d`（同日官方 tip）；当前分支相对其为本地独有 153、upstream 独有 4,996 commits，不能盲目 merge/rebase。已审阅 Kimi K3/KDA、Qwen3.5 GDN、Mamba hybrid cache：借鉴 decode-only guard、persistent-state 原地 mutation、固定输出 buffer、warm-up 和 fallback；不移植其不同数学的 recurrent kernels。官方当前没有名为 Qwen3.6 或 Qwen3.8 的同名 model implementation。
5. [x] **官方 upstream 四次核查（2026-08-21）**：已再次执行 `git fetch upstream --prune`，最新 `upstream/main=1baf372bfc14d739860b4a7122877e22ef0dcbf1`；当前分支相对其 local-only 168、upstream-only 5,018 commits，禁止盲目 merge/rebase。复核的可借鉴路径仍是 `vllm/v1/worker/gpu/model_states/mamba_hybrid.py`、`vllm/v1/worker/mamba_utils.py`、`vllm/models/kimi_k3/nvidia/kda.py` 与 `vllm/model_executor/layers/mamba/gdn/qwen_gdn_linear_attn.py`：可借鉴 persistent GPU state metadata、direct state mutation、固定 buffer / warmup / fallback 纪律；Kimi GDN/KDA recurrent 数学和 Qwen/Kimi merged-projection weight layout 与 RWKV7 的严格 BF16 GEMV + FP32 recurrent state contract 不兼容，不能直接复制 kernel 或盲目 merge。
6. [ ] 将每个有效改动按阶段提交：有意义的 commit message、`Assisted-by: Codex` trailer、完整测试与 benchmark record；不要 push remote。
8. [x] **online FP8 量化探索（拒绝）**：`--quantization fp8` 仍可 Full Decode CUDA Graph，C=1 `30.89 TPS`（约 +17.9%）且 C=128 `1218.58 TPS`（约 +1.11%）；但相对严格服务 8×16 gate 出现 `5/8` text/token 分歧、selected logprob max `3.20997`、common Top-K max `13.64159`。这是模型质量变化，不是可接受的末位误差；稳定 BF16 服务已恢复。证据：`benchmark_records/20260821_fp8_online_quantization_rejected/`。
9. [x] **FP16 dtype 探索（拒绝）**：`--dtype float16` 相对 BF16 strict trace 仍有 `2/8` text/token 分歧（selected max `2.70194`），且没有速度收益：C=1 `26.17 TPS`（-0.16%）、C=128 `1198.66 TPS`（-0.55%）。Albatross 的 FP16 standalone 表现不能通过仅切换 vLLM dtype 获得。证据：`benchmark_records/20260821_fp16_dtype_rejected/`。
7. [x] **稳定服务 / 外部复测（2026-08-21）**：`15330b229` 服务已在 `127.0.0.1:8030` 以 direct-cache 开关启动，`/health` 与 `/v1/models` 成功；外部 dashboard POST 在 `20:10:34` 成功结束。另以 512 decode tokens × 3 的真实 OpenAI completions 复测 steady C=1 为 `27.181 / 27.199 / 27.188 TPS`，说明 32-token 基准的约 26 TPS 并不是主要由首 token 启动开销造成。

## 7. 构建约束

- 禁止使用系统 `python3` 或裸 `pip`。当前可运行测试环境是 vllm-sp 的 `/mnt/data/anaconda3/envs/vllm-sp/bin/python`（Python 3.11.15；仓库 `.venv/bin/python` 当前误指向不含 torch 的 base Python，修复前不得用它替代 vllm-sp）。服务启动沿用 `/mnt/data/anaconda3/envs/vllm-sp/bin/vllm`。
- C++/CUDA 变更前使用 vllm-sp 的 compiler/CUDA 变量，并通过既有 `/tmp/vllm-rwkv7-exact-build` 构建 `_C.abi3.so`；构建后再运行所有精度和服务测试。
- 任何候选的默认开关必须可显式关闭并在不满足 layout/dtype/device guard 时 fallback。
- 提交前检查 `git status --short`、focused suite、精度 A/B、C=128 benchmark 和 `benchmark_records` 完整性。


## 8. 下一步优先级（2026-08-21 upstream/混合线性模型复核）

1. [ ] **最高优先：C=1 BF16 projection/GEMV 的 strict-compatible 方案**。C=1 CUDA trace 中 `internal::gemvx` 约占 GPU 时间 82%，约 426 个 projection/step；recurrent cache 已不是 50+ TPS 的主瓶颈。任何 grouped/low-rank launch 编排必须逐 projection `torch.equal`，不得改变现有 BF16 GEMV reduction/cast/materialization 合约；完成后再做 8×16 service logprob 与 C=1/C=128 成对基准。
2. [ ] **`mamba_cache_mode=all` 的 RWKV src/dst direct-cache recurrence**。借鉴 Mamba `state_batch_indices`/`dst_state_batch_indices` 与 Kimi KDA `initial_state + state_indices` 接口：从 source persistent FP32 row 做既有 exact recurrence，写 destination row；必须覆盖 src=dst、src≠dst、outer stride、padding 只读 slot 0/绝不写、duplicate dst guard。预期只改善 cache-all/prefix/跨 block 场景，不宣传为当前 align steady decode 提速。
3. [ ] **shared state-slot metadata hoist → GPU-only align-state migration**。先验证 shared integer slot metadata 与每层现有逻辑逐项相同（C=1/C=128、prefix resume、block boundary、padding、align/all）；目标是在 Full Graph 中减少每层重复 `clamp/gather/squeeze` 节点。随后才评估移植 upstream `vllm/v1/worker/gpu/model_states/mamba_hybrid.py` + `vllm/v1/worker/mamba_utils.py` 的 GPU-only byte-copy lifecycle：copy 必须对 shift/recurrent/FFN 三类 state 均 `torch.equal`，padding lane 永不写有效 slot。它主要改善 prefix / block boundary / speculative 状态迁移，不应宣传为 C=1 GEMV 突破；当前 upstream=`1baf372bfc`（2026-08-21 已 fetch），禁止盲目 merge/rebase。
4. [x] **Albatross sparse CMix 结论**：有 C=1 性能信号但当前 BF16 ordered sparse reduction 仍不 exact（服务 token/logprob 分歧），继续保持拒绝。只有 isolated `torch.equal(sparse, dense F.linear)`、multi-token full-cache 和服务 gate 全部为零差异才可重启该方向。

### 2026-08-21 shared align state-index metadata hoist（拒绝，已恢复）

- [x] 参考官方 upstream 截点 `574e6a00` 的 Bailing hybrid linear-attention
  persistent metadata 思路，验证了 RWKV7 的最小严格候选：layer 0 通过一个
  in-place CUDA op 从每次 replay 更新后的 `seq_lens + block_table_tensor` 生成
  state slot，后续 60 层复用同一个 scratch，从而消除 61 层重复的 graph
  `clamp + gather`。BF16 projection、FP32 recurrent state、reduction 顺序、
  direct-cache 写入和 PAD store contract 均不变。
- [x] 候选 CUDA unit（int32/int64、零长度 padding、block boundary、strided
  output）`4 passed`；Full Decode capture sizes `1,2,4,8,16,32,64,128` 完成；
  `8 prompts × 16 greedy, logprobs=20` 服务 trace 与
  `/tmp/rwkv7_service_trace_before_aux.json` 字节级零差。
- [x] restart-paired HTTP：C=1 `28.4512 → 28.6072 TPS`（+0.55%），C=128
  aggregate `1565.3460 → 1573.9400 TPS`（+0.55%）。低于 1% materiality gate，
  已完整恢复源码与扩展；记录：
  `benchmark_records/20260821_shared_align_state_indices_candidate/`。
- [x] 恢复稳定服务后已 POST `http://192.168.10.121:3000/api/run-test`，
  dashboard 在 **2026-08-21 17:36:48** 成功完成、保存 8 条记录。

### 2026-08-21 官方 upstream / 混合线性模型审查

- [x] 已于 2026-08-21 再次执行 `git fetch upstream --prune`；当前官方远端为
  `0a3a4f7f35b7e6e70a1804742dec65b6bec1d2fa`。当前分支相对其
  `ahead 191 / behind 5032`，禁止盲目 merge/rebase。该新增区间中仅发现一个
  可借鉴但不直接适用于 RWKV7 的 K3 MXFP4 top-k finalization 融合；没有新的
  RWKV7 / generic linear-attention decode kernel 可安全移植。
- [x] 可借鉴的实现是 Bailing/Mamba/KDA 的 **persistent CUDA-Graph metadata、
  PAD slot 与 GPU state 生命周期**；不能直接复制 Kimi KDA/GDN recurrence 或
  Qwen merged projection，因为 RWKV7 必须保留 BF16 `F.linear` reduction
  contract 与完整 FP32 recurrent matrix state。
- [ ] 下一主线（按收益优先级）：严格兼容的 BF16 GEMV/projection 优化。历史
  profile 显示 C=1 约 82% CUDA 时间在 `internal::gemvx`（约 426 个 linear-like
  calls/token）；metadata/cache/scheduler 微调不可能把约 28.5 TPS 推到 50+。
  需以 layer/projection group 为单位建立 exact-output A/B，优先探索不改变
  PyTorch BF16 reduction 的 persistent GEMV scheduling、stream overlap 与
  可验证的 cublasLt algorithm cache；任何 FP16 Albatross GEMV、sparse CMix 或
  merged-GEMM 路径必须先通过长 decode logprob 门禁才可能作为可选精度档。
- [ ] correctness hardening（非主要 TPS 项）：为 padded Full Decode 的
  `PAD_SLOT_ID` 做端到端 gather/direct-cache 回归，覆盖 C=127→128、多 replay、
  slot 0 不写入；先验证现有 full-fusion/direct-cache 的 -1 slot contract，避免
  因纯性能改动引入隐患。

### 2026-08-21 multistream K/A → kk-pre early release（拒绝，已恢复）

- [x] 在已保留的 C=1 balanced auxiliary 三 child-stream 路径上验证了一个严格
  event-DAG 候选：K→A 完成后记录 event，main stream 用原 BF16→FP32 cast 和原
  `kk_pre` 与该 child stream 后续独立 W 工作、R+G/V+V-gate 工作重叠。没有改动
  任一 `F.linear`、activation、dtype、reduction、recurrent/direct-cache 或 cache
  写入；仅增加 `RWKV7_USE_MULTISTREAM_EARLY_KK_PRE=1` 的 A/B 路径。
- [x] layer 0/1 strict tensor test 通过；Full Decode graph capture
  `1,2,4,8,16,32,64,128` 完成；8×16 `logprobs=20` 服务 trace 字节级零差。
- [x] restart-paired HTTP：C=1 `28.4527 → 28.6101 TPS`（+0.55%），C=128
  aggregate `1564.9534 → 1569.4818 TPS`（+0.29%）；低于 1% gate，已恢复。
  记录：`benchmark_records/20260821_multistream_early_kk_pre_candidate/`。

### 瓶颈口径修正

- [x] “约 426 个 linear-like profiler events/token”是历史 profile 的筛选后
  kernel-event 口径，不应当成源码 `F.linear` 调用数。目标 61 层、C=4096 的
  direct-linear block path 静态计数为 **852 个 block-internal F.linear/token**：
  layer 0 为 12 个，layers 1–60 各 14 个（另不含最终 lm-head/sampling）。
  C=1 到 50+ TPS 的唯一高收益主线仍是保持输出 contract 的 M=1 BF16 GEMV；
  metadata、cache 和纯 event 调度只能提供个位数百分比空间。

### 2026-08-21 BF16 M=1 GEMV shape baseline / output-row split（拒绝）

- [x] 停止服务释放 GPU 后，建立目标 RTX 4090 D / SM89 的 exact isolated
  `F.linear` 基线：`4096×4096` attention R/K/V/O 约 `17.32 µs`；FFN up
  `16384×4096` 约 `155.97 µs`；FFN down `4096×16384` 约 `157.64 µs`；128/192/
  384-rank LoRA 各段约 `7.72–7.89 µs`。852 个 block-internal projection 的
  shape 加权下界约 `29 ms/token`，与 C=1 服务约 28.5 TPS 相符。
- [x] 测试严格的 output-row split（不拆单个 dot-product reduction）：部分二分
  路径可 `torch.equal`，但仍回退——FFN up `133.17 → 136.01 µs`、FFN down
  `157.20 → 160.23 µs`、attention `16.08 → 57.50 µs`；四分 FFN-up 已不再
  bit-exact（不同 output shape 选中不同 BF16 GEMV reduction）。不接入。
- [x] 这给出 50 TPS 的量化下限：20 ms/token 目标下，即便剩余非 projection
  约 6 ms 完全不变，也必须将 projection 约 29 ms 压到约 14 ms，需 **约 52%
  reduction**。因此下一项必须是少 launch 的 shape-specialized BF16 GEMV/
  projection backend；单纯 row split、event、metadata 或 cache 优化均不足。
  记录：`benchmark_records/20260821_bf16_gemv_shape_baseline/`。

### 2026-08-21 native BF16 LoRA-input GEMV（精度通过、性能拒绝）

- [x] 实现并验证过一个严格 M=1 CUDA BF16 GEMV 原型，仅覆盖 RWKV7 无 bias 的
  LoRA input projection `[1,4096] × [128|192|384,4096]^T`；BF16 input/weight、
  FP32 FMA、BF16 output rounding，不触碰 recurrent state。isolated 约 `1.46–1.50×`
  快于 `F.linear`（例如 4096×4096 `17.39 → 11.92 µs`、LoRA input 约
  `8.02 → 5.33 µs`）。
- [x] 精度没有被轻易放弃：8 个随机 BF16 input 的目标 shape CUDA unit `3 passed`；
  真实 checkpoint 的全部 243 个 LoRA-input 权重（122×192、61×384、60×128）各
  8 个 BF16 input 的 `torch.equal` 检查为 **0 different elements**；服务 8×16
  `logprobs=20` trace 字节级零差，Full Decode Graph 全 capture size 正常。
- [x] 仍不保留的原因是端到端性能，而非精度：restart-paired C=1
  `28.4445 → 28.1004 TPS`（**-1.21%**）；C=128 不触发 M=1 kernel，
  `1565.1864 → 1566.7799 TPS`（+0.10% 噪声）。独立 kernel 的 output allocation/
  graph integration 成本抵消了 low-rank kernel 节省；源码、扩展和服务均已恢复。
  下一次 GEMV 实验必须采用 persistent output buffer 或融合紧随的 activation/
  second low-rank projection，单独 allocating op 不再重复。记录：
  `benchmark_records/20260821_native_bf16_lora_input_gemv_candidate/`。

### 2026-08-21 persistent-output native BF16 LoRA-input GEMV（精度通过、性能拒绝）

- [x] 为排除上一轮 standalone custom GEMV 的 allocator 成本，进一步实现过
  persistent-output out-variant：每个 `RWKV7LoRA` 拥有 CUDA-Graph-stable
  `[1,rank]` BF16 scratch，kernel 直接写入，后续原 activation 和原 second
  `F.linear` 消费；不再在 graph replay 分配 output。
- [x] 目标 shape out-op CUDA unit `3 passed`；Full Decode Graph
  `1,2,4,8,16,32,64,128` 正常；服务 8×16 `logprobs=20` trace 字节级零差。
- [x] restart-paired 仍回退：C=1 `28.4484 → 28.0959 TPS`（**-1.24%**），
  C=128 `1567.2270 → 1562.4224 TPS`（-0.31%；M=1 路径不应触发）。结论是
  allocator 不是根因，standalone low-rank custom GEMV 在实际 graph/stream
  调度中本身不如当前 F.linear；已完整恢复。后续不再重复单独 LoRA-input
  replacement，必须尝试同时减少 launch 的 complete producer-consumer fusion，
  且保持原 BF16 reduction/activation rounding contract。记录：
  `benchmark_records/20260821_native_bf16_lora_input_gemv_persistent_candidate/`。

### 2026-08-21 FP16 model-dtype candidate（拒绝，BF16 严格服务已恢复）

- [x] 为按 Albatross 的 FP16 方向继续压低精度误差而不是直接假设不可行，保持所有
  Full Decode CUDA Graph、exact FP32 direct-cache recurrent、native gather/store 与多
  stream 开关不变，仅将服务模型 dtype 从 `bfloat16` 改为 `float16` 做了完整服务级
  A/B。
- [x] 8 prompts × 16 greedy、`logprobs=20` 对严格 BF16 基线出现 **2/8** text/token
  分歧、`25/128` Top-1 分歧；selected-token logprob 最大误差 `2.70193577`，common
  Top-K 最大误差 `10.36004497`。这不是可通过轻微阈值放宽接受的误差：递归 decode
  已发生 token 路径改变。
- [x] paired HTTP 性能也没有收益：C=1 `28.4581 TPS` 对恢复后的严格 BF16
  `28.4539 TPS`（`+0.01%`）；C=128 aggregate `1557.3263` 对 `1562.6169 TPS`
  （`-0.34%`）。因此 FP16 既不满足精度，也不能提升当前 vLLM graph 路径。
- [x] 已恢复稳定 BF16 服务，恢复后 trace 与 `/tmp/rwkv7_service_trace_before_aux.json`
  **byte-identical**。证据：`benchmark_records/20260821_fp16_model_dtype_candidate/`。

### 当前下一主线（2026-08-21）

- [ ] 目标 C=1 50+ TPS 需要从当前约 `28.45 TPS` 缩短约 `35.1 → 20 ms/token`。61 层
  direct-linear 路径的约 852 个 block-internal BF16 `F.linear` 是主耗时；严格
  direct-cache/metadata/stream-only 路线已经接近个位数百分比上限。
- [ ] 不重复已拒绝的 standalone LoRA-input GEMV。优先评估能保留单个 GEMV
  reduction contract、但减少跨算子 launch/中间读写的 **complete producer-consumer
  fusion**：先从 FFN `key → SqReLU → value` 和 LoRA `linear → activation → linear`
  的 compile/fusion feasibility 开始，必须按 isolated `torch.equal`、模型张量、长
  decode、服务 logprob、Full Graph、C=1/C=128 逐层门禁。
- [ ] Albatross 仅作为 B=1/T=1 的 tile、数据驻留和 launch 编排参考；其 FP16 +
  custom reduction + fast-math mega-kernel 与本仓库 BF16 projection + FP32 recurrent
  的 strict output contract 不兼容，不能直接作为默认路径。若未来增加 non-strict
  precision tier，必须显式分离、默认关闭并报告 token/logprob 误差。

### 2026-08-21 FFN producer-consumer `torch.compile` 可行性（拒绝）

- [x] 在修改模型前先单独验证了目前最小的严格 FFN fusion 假设：`F.linear(4096→16384)
  → relu² → F.linear(16384→4096)`。native `torch.relu().square()` 与当前 vLLM C++
  `relu2` CUDA custom op 输出 `torch.equal`；`torch.compile(fullgraph=True)` 输出也
  `torch.equal`，因此没有因数值误差提前放弃。
- [x] 但 RTX 4090 D / SM89 的 50 次 steady CUDA-event 测量为：当前 eager custom path
  `0.32584 ms`，native expression `0.32664 ms`，compiled expression `0.35719 ms`
  （**+9.62% 更慢**）。Inductor 没有把两个 cuBLAS BF16 GEMV 与 activation 降成有利的
  fused execution，且当前全模型 graph 已消除了大部分 host launch 成本。
- [x] 因此不在模型中添加 compile flag，也不重复全模型 compile 的无收益路径。下一项
  complete producer-consumer 融合必须实际减少 launch 或中间读写，同时逐 dot-product
  保持 BF16 reduction contract。记录：
  `benchmark_records/20260821_ffn_compile_fusion_feasibility/`。

### 2026-08-21 外部 dashboard 回归（严格 BF16 服务）

- [x] 已在恢复 strict BF16 Full Decode CUDA Graph 服务后 POST
  `http://192.168.10.121:3000/api/run-test`。最终 GET `/api/status` 为
  `running=false`、`progress=1.0`、`Test completed: 8 records saved`，完成时刻为
  **2026-08-21 18:27:59**，C=1…128 八个点全部 100% success。
- [x] dashboard 的 C=1 输出/整体为 `39.1 / 38.5 t/s`；C=128 的**每请求**输出/整体为
  `17.9 / 17.3 t/s`，TTFT `1477 ms`。同一 C=128 区间 vLLM engine 的 10 秒窗口
  aggregate generation 达约 `1.69–1.70k tokens/s`。每请求指标会随并发共享 GPU 而下降，
  不应与固定 payload 的 C=128 **aggregate** `~1563 TPS` 混为一个口径。
- [x] HTML、最终状态及口径说明：
  `benchmark_records/20260821_dashboard_bf16_restore/`。

### 2026-08-21 四路 auxiliary projection stream（严格通过、低收益拒绝）

- [x] 继续榨取 C=1 独立 projection 的并发度，测试 `R+G | K+W | V+V-gate | A` 四条
  persistent CUDA child streams；相比保留的三路拓扑，仅将完全独立的 A rank-192 LoRA
  从 `K+W+A` 拆出。没有改变任何 BF16 GEMV 的 operand/dtype/reduction、activation、
  cache 或 FP32 recurrence。
- [x] 新增 layer 0/1 的 three-way/four-way `torch.equal` CUDA 测试并通过（`1 passed`）；
  Full Decode Graph 完成；8×16 service `logprobs=20` 为字节级零差异。
- [x] clean-restart paired throughput 确实有可复现 C=1 提升：32-token `28.4534 →
  28.6821 TPS`（`+0.804%`）、512-token `28.5628 → 28.7910 TPS`（`+0.799%`）；但 C=128
  aggregate 仅 `+0.173%`。低于 1% 保留门槛且增加第四套 stream/event topology，不能实际
  推进 50+ TPS，已将源码/测试恢复为三路稳定路径。记录：
  `benchmark_records/20260821_multistream_aux_4way_candidate/`。

### 2026-08-21 selective native LoRA-input GEMV（W+A，严格通过、拒绝）

- [x] 为继续定位而非笼统放弃此前 strict native LoRA-input GEMV，恢复其临时
  out-variant 并只启用于原本最可能是关键路径的 `K+W+A` child stream：W/A 的
  `4096→192` 第一段 BF16 projection；G/V 和所有第二段 projection 均保持 `F.linear`。
  临时 fragment 仅从 `/tmp` 显式加载，绝不进入生产启动命令。
- [x] Full Decode Graph `1..128` 正常；8×16 `logprobs=20` 服务 trace 完全字节一致：
  text/token/Top-1/selected/common Top-K 全为零差异。
- [x] 但 C=1 `28.4534 → 28.0572 TPS`（**-1.39%**）。C=128 路径不触发 M=1 op，
  数值不能归因。它证明不是 G/V 支路或 all-module 选择造成此前回退，而是 native
  out-op 在真实 graph/stream 上的 integration 成本本身超过 isolated GEMV 节省；已恢复
  源码和严格稳定服务。记录：
  `benchmark_records/20260821_selective_native_lora_wa_candidate/`。

### 2026-08-21 FFN sparse exact-reduction feasibility（拒绝）

- [x] 针对此前 sparse CMix 的精度问题继续做了“先降误差”实验：恢复历史 strict
  native BF16 GEMV object 并确认其 FFN up/down 可与 PyTorch `F.linear` 逐元素一致，
  但 dense FFN up `165.30→167.71 µs`、down `166.78→173.12 µs` 均更慢，不能作为
  dense replacement。
- [x] 随后实现仅临时使用的 FFN-down skip-zero BF16/FP32-FMA CUDA prototype，试图在
  保持相同 reduction 前提下利用 SqReLU 稀疏性。约 94% zero 时仍有 2 个 BF16 element
  不同且 `159.74→218.65 µs`，甚至 90% zero 也 `160.11→235.51 µs`；只有极端约 98%
  zero 才接近平局。因此既不稳定 exact，也不够快，未接入模型。
- [x] 这进一步缩小路径：要利用 Albatross CMix 稀疏性，必须同时重建 PyTorch/cuBLAS
  reduction tree 和高效 multi-output tile；简单 warp-per-row 或跳过 zero 权重读取无法
  达标。临时 extension/代码均未进入仓库。记录：
  `benchmark_records/20260821_ffn_sparse_exact_reduction_probe/`。

### 2026-08-21 fused BF16 decay-scale / cast preparation（拒绝）

- [x] 试验 `RWKV7_USE_FUSED_W_SCALE_CAST=1`：把 direct CUDA path 中 `w = LOG_DECAY_SCALE * sigmoid(w_lora(...))` 的 BF16 标量乘并入已保留的 `rwkv7_cast_kk_pre` Triton kernel。kernel 明确执行 FP32 multiply → BF16 writeback语义 → FP32 recurrent input，避免直接 FP32 multiply 改变 eager BF16 materialization；每个 projection、sigmoid、state/reduction 与 Full Decode CUDA Graph 路径均保持不变。
- [x] 精度门禁通过：B=1/8/128 `rwkv7_cast_kk_pre` 与 attention recurrent-input bundle 均 `torch.equal`；focused CUDA tests `6 passed`；8 prompts × 16 greedy、`logprobs=20` 服务 JSON trace byte-identical。
- [x] **不保留**：C=1 32-token `28.9459 → 28.9515 TPS`（+0.019%）；idle-GPU repeat 的 C=128 aggregate `1609.28 → 1606.44 TPS`（-0.177%）。首个 candidate C=128 sample 被外部干扰污染（873.55 TPS）并保留原始记录但不用于结论。该操作虽严格正确，省掉的单个 scale launch 对完整 graph 无材料级收益；源码/服务已恢复稳定配置。证据：`benchmark_records/20260821_fused_w_scale_cast_rejected/`。
